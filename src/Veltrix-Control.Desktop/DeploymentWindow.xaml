<Window x:Class="VeltrixControl.Desktop.DeploymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New deployment" Width="620" Height="620" MinWidth="520" MinHeight="520"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel>
      <TextBlock x:Name="PackageText" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap"/>
      <TextBlock x:Name="PackageDetail" Foreground="{DynamicResource MutedTextBrush}" Margin="0,4,0,16"/>
      <Label Content="Action"/>
      <ComboBox x:Name="ActionBox" HorizontalAlignment="Left" Width="200">
        <ComboBoxItem Content="Install" IsSelected="True"/>
        <ComboBoxItem Content="Uninstall"/>
        <ComboBoxItem Content="Upgrade"/>
      </ComboBox>
      <TextBlock Text="Target devices" FontWeight="SemiBold" Margin="0,18,0,6"/>
      <TextBlock Text="Select one or more enrolled devices. Each device receives an audited installation operation." Foreground="{DynamicResource MutedTextBrush}" Margin="0,0,0,8"/>
    </StackPanel>
    <ListBox x:Name="DevicesList" Grid.Row="2" SelectionMode="Extended" DisplayMemberPath="Name"/>
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Cancel" IsCancel="True" Margin="0,0,10,0"/>
      <Button Content="Deploy" Style="{StaticResource PrimaryButton}" Click="Deploy_Click"/>
    </StackPanel>
  </Grid>
</Window>
